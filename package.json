{
  "name": "kayce.basqu.es",
  "version": "2.0.0",
  "scripts": {
    "build": "npx @11ty/eleventy;",
    "deploy": "npm run build; npx firebase deploy;"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.2",
    "firebase-tools": "^11.9.0"
  }
}
