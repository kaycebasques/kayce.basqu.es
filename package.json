{
  "name": "kayce.basqu.es",
  "version": "0.0.0",
  "scripts": {
    "deploy": "npx @11ty/eleventy; npx firebase deploy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.2",
    "firebase-tools": "^11.9.0"
  }
}
